# OpenAI Configuration
# Required: Your OpenAI API key for AI completions and translations
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Custom OpenAI API endpoint (defaults to https://api.openai.com/v1)
# Useful for OpenAI-compatible services like Azure OpenAI, local models, etc.
OPENAI_BASE_URL=https://api.openai.com/v1

# Authentication & User Management
# Optional: Email address for authentication bypass and admin privileges
# When set, this email will have full access and admin rights
# When not set, the app runs in proxy mode forwarding to official Raycast backend
RAYCAST_EMAIL=your-email@example.com

# Storage Configuration
# Optional: Vercel KV storage name for data persistence
# Required only if you want to use sync functionality
# Leave empty if you don't need data synchronization features
KV_NAME=your-kv-store-name

# Vercel KV Configuration (if using Vercel KV)
# These are automatically provided by Vercel when you add KV storage
# You typically don't need to set these manually
# KV_URL=your-kv-url
# KV_REST_API_URL=your-kv-rest-api-url
# KV_REST_API_TOKEN=your-kv-rest-api-token
# KV_REST_API_READ_ONLY_TOKEN=your-kv-rest-api-read-only-token